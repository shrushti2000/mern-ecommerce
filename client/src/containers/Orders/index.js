import React from 'react';
import Layout from '../../components/Layout';


const Orders = (props) => {
    return (
        <div>
            <Layout sidebar>
                orders
            </Layout>
        </div>
    )
}

export default Orders
